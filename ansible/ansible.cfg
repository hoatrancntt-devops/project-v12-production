[defaults]
inventory      = inventory/hosts.yml
roles_path     = roles
host_key_checking = False
timeout        = 30
remote_user    = ubuntu

[privilege_escalation]
become = True
become_method = sudo

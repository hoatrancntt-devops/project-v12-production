all:
  children:
    ec2:
      hosts:
        EC2_PUBLIC_IP:                # ← Tự động cập nhật sau terraform output
          ansible_user: ubuntu"
          ansible_ssh_private_key_file: ~/.ssh/id_project_v12"
    proxmox_vm:
      hosts:
        172.199.10.180":
          ansible_user: ubuntu"
          ansible_ssh_private_key_file: ~/.ssh/id_project_v12"
